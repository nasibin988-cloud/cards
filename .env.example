# Copy this file to `.env` next to docker-compose.yml on the Hetzner box.
# `.env` is gitignored so the token never leaves the server.
#
# Generate a fresh token: `openssl rand -hex 32`

CARDS_SYNC_TOKEN=replace-me-with-a-long-random-string
