import { default_w, default_request_retention, default_maximum_interval } from 'ts-fsrs';

export const DEFAULT_W: readonly number[] = default_w;
export const DEFAULT_RETENTION = default_request_retention;
export const DEFAULT_MAX_INTERVAL = default_maximum_interval;

export const DEFAULT_NEW_PER_DAY = 1000;
export const DEFAULT_REVIEWS_PER_DAY = 1000;
